spring:
  application:
    name: cloud-orders #服务器名称 必须有 需要到nacos注册
  cloud:
    # nacos的配置
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: nacos

# 端口号
server:
  port: ${port:8081}
